import { PaletteMode } from "@mui/material";

export const lightTheme = {
  palette: {
    mode: "light" as PaletteMode,
    background: {
      default: "#f5f5f5",
      paper: "#ffffff",
    },
  },
};